# Copy to ./env before running services

# Database
DATABASE_URL=sqlite:///./estim.db   # simple local file DB for now
POSTGRES_USER=estim
POSTGRES_PASSWORD=estim
POSTGRES_DB=estim

# Playwright
PLAYWRIGHT_HEADLESS=true
PLAYWRIGHT_BROWSER=chromium
PLAYWRIGHT_BROWSERS_PATH=0

# API
API_PORT=8000
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

